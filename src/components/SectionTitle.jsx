

const SectionTitle = ({heading}) => {
    return (
        <div>
            <h3 className="text-center text-[#023b6d] text-3xl font-extrabold uppercase my-10">{heading}</h3>
            
        </div>
    );
};

export default SectionTitle;