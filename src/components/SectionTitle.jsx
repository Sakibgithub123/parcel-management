

const SectionTitle = ({heading}) => {
    return (
        <div>
            <h3 className="text-center text-3xl font-extrabold uppercase my-5">{heading}</h3>
            
        </div>
    );
};

export default SectionTitle;